class NotificationService:
    @staticmethod
    def send_notification(user_id, message):
        # Placeholder for notification logic (e.g. Email, In-app)
        print(f"Notification to User {user_id}: {message}")
        return True
